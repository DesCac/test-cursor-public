# 🔧 Инструкции по пересборке фронтенда

## Что было исправлено:

### ✅ Критические изменения:
1. **Убран `scoped` из всех Vue компонентов** - это блокировало применение стилей к VueFlow
2. **Добавлены принудительные стили видимости** для узлов в `app.css`
3. **Настроены глобальные стили VueFlow** для edges, nodes, handles
4. **Улучшены фикстуры** - сложные разветвлённые схемы с условиями
5. **ИСПРАВЛЕНА КРИТИЧЕСКАЯ ПРОБЛЕМА #1**: Добавлены импорты CSS в entry points (`npc-editor.js` и `quest-editor.js`)
6. **ИСПРАВЛЕНА КРИТИЧЕСКАЯ ПРОБЛЕМА #2**: Все стили узлов перенесены в `app.css` с `!important` для гарантированного применения
7. **ИСПРАВЛЕН ПОРЯДОК ЗАГРУЗКИ CSS**: VueFlow стили загружаются первыми, наши стили последними (для перекрытия)

## 📋 Команды для применения изменений:

### Вариант 1: Через Docker Compose (рекомендуется)

```bash
# Перезагрузить фикстуры
docker compose exec php php bin/console doctrine:fixtures:load --no-interaction

# Пересобрать фронтенд
docker compose exec php npm run build

# Перезапустить контейнеры (опционально)
docker compose restart
```

### Вариант 2: Через Makefile

```bash
# Загрузить фикстуры
make fixtures

# Или всё сразу
make install
```

### Вариант 3: Локально (если не используете Docker)

```bash
# Загрузить фикстуры
php bin/console doctrine:fixtures:load --no-interaction

# Пересобрать фронтенд
npm run build
```

## 🌐 После пересборки:

1. Откройте браузер
2. **Обновите страницу с очисткой кэша**: `Ctrl+Shift+R` (Windows/Linux) или `Cmd+Shift+R` (Mac)
3. Откройте редактор NPC: `http://localhost:8080/admin/npcs/3/edit` (Elder Sage)

## ✨ Что вы должны увидеть:

- ✅ **Белые узлы** с цветными заголовками и рамками
- ✅ **Фиолетовые линии** связей между узлами
- ✅ **Сложную схему** Elder Sage с 19 узлами и множественными ветвлениями
- ✅ **MiniMap** в правом нижнем углу
- ✅ **Controls** (zoom) в левом верхнем углу
- ✅ **Библиотеку узлов** слева с типами: Старт, Диалог, Выбор, Действие, Условие, Завершение

## 🐛 Если проблемы остались:

1. Откройте консоль разработчика (`F12`)
2. Проверьте вкладку Console на ошибки
3. Проверьте вкладку Network - убедитесь что все CSS файлы загрузились
4. Убедитесь что в Elements видны узлы с классом `.logic-node`

## 📊 Тестовые NPC:

1. **Elder Sage** (ID: 3) - сложный разветвлённый квест с:
   - Проверкой уровня игрока
   - Условными переходами
   - Секретным путём с древним ключом
   - Множественными концовками

2. **Merchant Bob** (ID: 4) - торговец с:
   - Покупкой/продажей
   - Ремонтом снаряжения
   - Проверкой золота
   - Циклическими диалогами

## 🔍 Структура изменённых файлов:

```
assets/
├── styles/app.css                    ✏️ Добавлены VueFlow стили
├── components/
│   ├── NPCEditor.vue                 ✏️ Убран scoped, улучшен VueFlow
│   ├── QuestEditor.vue               ✏️ Убран scoped, улучшен VueFlow
│   └── flow/
│       ├── LogicNode.vue             ✏️ Убран scoped, новый дизайн
│       ├── Palette.vue               ✏️ Убран scoped
│       └── PropertiesPanel.vue       ✏️ Убран scoped

src/
└── DataFixtures/
    └── NPCFixtures.php               ✏️ Новые сложные схемы
```
