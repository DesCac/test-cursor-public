# 🎮 Руководство по навигации в GraphQL API для игровых графов

## Что такое этот API?

Это GraphQL API для управления **диалогами с NPC** и **квестами** в RPG-игре. Данные организованы в виде **графов** (узлов и связей).

## 📊 Структура графов

### Граф = Узлы + Связи

```
[Узел A] --связь--> [Узел B] --связь--> [Узел C]
           ↓
        [Узел D]
```

---

## 🗣️ Граф диалогов (DialogNodes)

### Типы узлов диалога:
- **`start`** - начало диалога
- **`dialog`** - реплика NPC
- **`choice`** - выбор игрока
- **`condition`** - проверка условий (уровень, предметы и т.д.)
- **`action`** - действие (дать предмет, начать квест)
- **`end`** - конец диалога

### Пример навигации по диалогу:

```graphql
query NavigateDialog {
  npc(id: 9) {
    name
    dialogNodes {
      id
      nodeType
      text
      conditions          # JSON: {"level": {"min": 5}}
      
      connections {       # ⬅️ Связи к следующим узлам
        id
        choiceText       # Текст выбора игрока
        conditions       # Условия для этой связи
        
        targetNode {     # ⬅️ Следующий узел
          id
          nodeType
          text
        }
      }
    }
  }
}
```

### Как перемещаться:

1. **Начните с узла типа `start`**
2. **Посмотрите на `connections`** - это доступные пути
3. **Выберите связь** (по `choiceText` или условиям)
4. **Перейдите к `targetNode`** - это следующий узел
5. **Повторяйте**, пока не дойдёте до узла типа `end`

---

## 🎯 Практический пример: Диалог со старейшиной

```
                    [94: start]
                        ↓
    "Приветствую тебя, путник..."
                        ↓
                    [95: dialog]
                        ↓
    "Наш город в опасности..."
                   ↙        ↘
    "Расскажи подробнее"  "Не интересует"
            ↓                    ↓
      [96: dialog]          [97: dialog]
            ↓                    ↓
   [98: condition]        [108: end]
   (Проверка уровня)
      ↙        ↘
  level<5    level>=5
     ↓          ↓
[99: dialog] [100: dialog]
     ↓          ↓
[109: end]  [101: action]
              ↓
         "Получить квест"
```

### Запрос для получения начального узла:

```graphql
{
  npc(id: 9) {
    dialogNodes {
      id
      nodeType
      text
      connections {
        choiceText
        targetNode {
          id
          text
        }
      }
    }
  }
}
```

---

## 📜 Граф квестов (QuestNodes)

### Типы узлов квеста:
- **`start`** - начало квеста
- **`objective`** - цель/задание
- **`condition`** - проверка условий
- **`reward`** - выдача награды
- **`end`** - завершение квеста

### Пример навигации по квесту:

```graphql
query NavigateQuest {
  quest(id: 9) {
    name
    description
    objectives       # JSON массив целей
    rewards          # JSON объект наград
    requirements     # JSON требования
    
    logicNodes {     # ⬅️ Узлы логики квеста
      id
      nodeType
      data           # JSON данные узла
      conditions     # JSON условия
      
      connections {  # ⬅️ Связи к следующим узлам
        id
        conditions
        
        targetNode { # ⬅️ Следующий узел
          id
          nodeType
          data
        }
      }
    }
  }
}
```

---

## 🔍 Алгоритм навигации по графу

### В игровом клиенте:

```python
# Псевдокод для навигации по диалогу

current_node = find_start_node(npc.dialogNodes)

while current_node.nodeType != "end":
    # Показать текст узла
    display(current_node.text)
    
    # Проверить тип узла
    if current_node.nodeType == "condition":
        # Проверить условия и выбрать подходящую связь
        connection = check_conditions(current_node.connections, player_state)
    
    elif current_node.nodeType == "dialog":
        # Показать варианты ответа игроку
        connection = player.choose(current_node.connections)
    
    elif current_node.nodeType == "action":
        # Выполнить действие
        execute_action(current_node)
        connection = current_node.connections[0]  # Обычно одна связь
    
    # Перейти к следующему узлу
    current_node = connection.targetNode

# Диалог завершён
display(current_node.text)
```

---

## 🎲 Примеры использования

### 1. Получить все доступные диалоги NPC:

```graphql
{
  npcs {
    id
    name
    description
  }
}
```

### 2. Получить начальный узел диалога:

```graphql
{
  npc(id: 9) {
    name
    dialogNodes {
      id
      nodeType
      text
      connections {
        id
        choiceText
        targetNode {
          id
        }
      }
    }
  }
}
```

### 3. Проверить условия перед переходом:

```graphql
{
  npc(id: 9) {
    dialogNodes {
      id
      nodeType
      conditions  # {"level": {"min": 5}, "inventory": {"has": "ancient_key"}}
      connections {
        conditions  # Условия для конкретной связи
        targetNode {
          id
          text
        }
      }
    }
  }
}
```

### 4. Валидация выбора в диалоге:

```graphql
{
  validateDialogChoice(npcId: 9, nodeId: 98, choiceId: 269) {
    valid          # true/false
    message        # Сообщение об ошибке
    nextNodeId     # ID следующего узла
  }
}
```

---

## 🛠️ Как это используется в игре?

### 1. **В редакторе диалогов** (NPC Editor):
   - Создавать и редактировать узлы диалога визуально
   - Соединять узлы линиями (connections)
   - Настраивать условия и действия

### 2. **В игровом клиенте**:
   - Загружать граф диалога при разговоре с NPC
   - Пошагово перемещаться по узлам
   - Показывать текст и варианты выбора игроку
   - Проверять условия (уровень, предметы)
   - Выполнять действия (дать предмет, начать квест)

### 3. **В редакторе квестов** (Quest Editor):
   - Создавать логику выполнения квеста
   - Настраивать условия завершения
   - Определять награды

---

## 📝 Полезные советы

### JSON поля возвращаются как строки:
```json
{
  "conditions": "{\"level\":{\"min\":5}}",
  "data": "{\"gold\":500,\"experience\":1000}"
}
```

Их нужно парсить в клиенте:
```javascript
const conditions = JSON.parse(node.conditions);
if (player.level >= conditions.level.min) {
  // Разрешить переход
}
```

### Узлы можно искать по ID:
В клиенте стройте хеш-таблицу для быстрого поиска:
```javascript
const nodesMap = {};
npc.dialogNodes.forEach(node => {
  nodesMap[node.id] = node;
});

// Быстрый переход к узлу
const nextNode = nodesMap[connection.targetNode.id];
```

---

## 🎨 Визуализация в UI

В редакторе (Vue.js компоненты уже есть):
- **LogicNode.vue** - отрисовка узлов
- **Palette.vue** - палитра для создания узлов
- **PropertiesPanel.vue** - редактирование свойств

Используется библиотека для работы с графами (например, VueFlow).

---

## 🚀 Следующие шаги

1. Откройте GraphiQL: `http://localhost:8080/graphiql.html`
2. Попробуйте примеры запросов
3. Изучите структуру узлов и связей
4. Создайте свой диалог через админ-панель
5. Протестируйте навигацию через API

**Удачи в создании игровых диалогов!** 🎮

